// endpoint

const ROOT = "/"

//USER
const API_USER = '/api/user'; 
const LOGIN = '/login'; 

//DIARY
const API_DIARY = '/api/diary';

const routes = {
  root:ROOT,
  apiUser : API_USER,
  login: LOGIN,
  apiDiary: API_DIARY
};

export default routes;
