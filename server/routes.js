// endpoint

const ROOT = "/"

//USER
const API_USER = '/api/user'; 
const LOGIN = '/login'; 
const AVATAR = '/avatar';

//DIARY
const API_DIARY = '/api/diary';

const routes = {
  root:ROOT,
  apiUser : API_USER,
  login: LOGIN,
  avatar: AVATAR,
  apiDiary: API_DIARY
};

export default routes;
